#move all globals here
# settings.py
#globals are neurons numpy parameters
#globals are list of tkinter grafical object corresponding to neurons
import numpy as np
#global variable
layer_amount = 10
max_neurons_inlayer = 0
#we should create the same amount of numpy arrays as layers
neurons_array = np.zeros([1,1]) #page 116 Rashid, create an array of shape 3 by 2